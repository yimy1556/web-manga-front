const backendUrl = `http://localhost:8080/api`;

/**
 * Frontend Url.
 */
const frontendUrl = {
  host: 'localhost:3000',
  protocol: 'http',
};

export default {
  frontendUrl,
  backendUrl,
};
